# DOCS Manifest

All documentation files are stored flat in this folder (no module-wise subfolders).

- Source files were hash-verified before removing originals.
- Filename collisions are preserved using suffix: `__from_<original_path>`.
